import { atom } from 'recoil';

export const productState = atom({
  key: 'product',
  default: false,
});
export const industriesState = atom({
  key: 'industries',
  default: false,
});
export const learningState = atom({
  key: 'learning',
  default: false,
});
export const supportState = atom({
  key: 'support',
  default: false,
});
export const companyState = atom({
  key: 'company',
  default: false,
});
export const salesforceState = atom({
  key: 'salesforce',
  default: false,
});
export const loginState = atom({
  key: 'login',
  default: false,
});
export const contactState = atom({
  key: 'contact',
  default: false,
});
export const regionState = atom({
  key: 'region',
  default: false,
});
export const anyModalOpenedState = atom({
  key: 'modals',
  default: false,
});
